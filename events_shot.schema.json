{
  "$id": "/events/shot",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Describes a ball hit by a player's paddle.",
  "properties": {
    "angle": {
      "description": "The angle at which the ball was hit.",
      "type": "number"
    },
    "ball": {
      "additionalProperties": false,
      "description": "stats related to how the ball moves as a result of being hit",
      "properties": {
        "crossed_net": {
          "description": "`true` if the ball crosses the net, else `false`.",
          "type": "boolean"
        },
        "direction": {
          "description": "Direction that the ball was hit from and to.",
          "enum": [
            "MidCrossLeft",
            "MidCrossRight",
            "DownTheMiddle",
            "LeftToMiddle",
            "LeftCrossRight",
            "DownTheLineLeft",
            "RightToMiddle",
            "RightCrossLeft",
            "DownTheLineRight"
          ],
          "type": "string"
        },
        "height_over_net": {
          "description": "Height the ball was over the net when it crossed.",
          "type": "number"
        },
        "max_height": {
          "description": "Max height of the ball in its arc (feet).",
          "type": "number"
        },
        "speed": {
          "description": "Speed of the ball in MPH.",
          "type": "number"
        },
        "spin": {
          "description": "The spin of the ball in RPM.",
          "type": "number"
        }
      },
      "required": [
        "speed",
        "max_height",
        "height_over_net",
        "direction",
        "crossed_net"
      ],
      "title": "Ball",
      "type": "object"
    },
    "distance": {
      "description": "The distance the ball traveled (feet) before coming in contact with the net, court, paddle, etc.",
      "type": "number"
    },
    "hit_zone": {
      "description": "Zone the ball was hit from.",
      "enum": [
        "deep",
        "mid",
        "short",
        "kitchen"
      ],
      "type": "string"
    },
    "is_volley": {
      "description": "whether this shot was a volley hit out of the air or not; omitted if we don't know",
      "type": "boolean"
    },
    "player_id": {
      "$ref": "/types/player_id",
      "description": "the player who hit the ball",
      "title": "Player ID"
    },
    "target_zone": {
      "description": "Zone the ball landed in or was hit from (whichever happens first)",
      "enum": [
        "deep",
        "mid",
        "short",
        "kitchen",
        "net",
        "out"
      ],
      "type": "string"
    },
    "type": {
      "enum": [
        "drop",
        "drive",
        "lob"
      ],
      "type": "string"
    }
  },
  "required": [
    "player_id",
    "ball",
    "hit_zone",
    "target_zone",
    "angle",
    "distance"
  ],
  "title": "Shot",
  "type": "object"
}