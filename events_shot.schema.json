{
  "$id": "/events/shot",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Describes a ball hit by a player's paddle.",
  "properties": {
    "is_volley": {
      "description": "whether this shot was a volley hit out of the air or not; omitted if we don't know",
      "type": "boolean"
    },
    "player_id": {
      "$ref": "/types/player_id",
      "description": "the player who hit the ball",
      "title": "Player ID"
    },
    "resulting_ball_movement": {
      "additionalProperties": false,
      "description": "stats related to how the ball moves as a result of being hit (omitted if we couldn't track the ball after the shot was made)",
      "properties": {
        "angles": {
          "additionalProperties": false,
          "properties": {
            "pitch": {
              "$ref": "/types/angle",
              "description": "The angle (in degrees) at which the ball was hit along the z plane (up/down).",
              "title": "Angle"
            },
            "yaw": {
              "$ref": "/types/angle",
              "description": "The angle (in degrees) at which the ball was hit in the xy plane (left/right).",
              "title": "Angle"
            }
          },
          "required": [
            "yaw",
            "pitch"
          ],
          "type": "object"
        },
        "crossed_net": {
          "description": "`true` if the ball crosses the net, else `false`.",
          "type": "boolean"
        },
        "direction": {
          "description": "Direction that the ball was hit from and to.",
          "enum": [
            "MidCrossLeft",
            "MidCrossRight",
            "DownTheMiddle",
            "LeftToMiddle",
            "LeftCrossRight",
            "DownTheLineLeft",
            "RightToMiddle",
            "RightCrossLeft",
            "DownTheLineRight"
          ],
          "type": "string"
        },
        "distance": {
          "description": "The distance the ball traveled (feet) before coming in contact with the net, court, paddle, etc.",
          "type": "number"
        },
        "end_zone": {
          "$ref": "/events/zone",
          "description": "Zone the ball bounced in or was next hit from (whichever happens first)",
          "title": "Zone"
        },
        "height_over_net": {
          "description": "Height the ball was over the net when it crossed.",
          "type": "number"
        },
        "max_height": {
          "description": "Max height of the ball in its arc (feet).",
          "type": "number"
        },
        "speed": {
          "description": "Speed of the ball in MPH.",
          "type": "number"
        }
      },
      "required": [
        "end_zone",
        "angles",
        "distance",
        "speed",
        "max_height",
        "height_over_net",
        "direction",
        "crossed_net"
      ],
      "title": "Resulting Ball Movement",
      "type": "object"
    },
    "start_zone": {
      "$ref": "/events/zone",
      "description": "where the ball was hit from",
      "title": "Zone"
    },
    "type": {
      "enum": [
        "drop",
        "drive",
        "lob"
      ],
      "type": "string"
    }
  },
  "required": [
    "player_id",
    "start_zone"
  ],
  "title": "Shot",
  "type": "object"
}